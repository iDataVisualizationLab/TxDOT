directories:
  output: dist
  buildResources: assets
files:
  - filter:
      - build/**/*
extraMetadata:
  main: build/electron.js
appId: comp.idvl.txdot
mac:
  category: public.app-category.productivity
win:
  target: portable
linux:
  target: AppImage
extends: react-cra
electronVersion: 8.2.0
